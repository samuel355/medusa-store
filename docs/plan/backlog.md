# Backlog

- Add integration tests against an ephemeral Medusa/PostgreSQL environment once CI infrastructure is available.
- Evaluate moving the root Next.js application to `apps/storefront` after commerce migration stabilizes.

